export EDITOR='vim'

alias cat='ccat'

alias ls='ls --color=auto'
alias ll='ls --color=auto -lAF'
alias la='ls --color=auto -laAF'

alias exit='echo "_" > ~/.zsh_history && exit'
alias poweroff='echo "_" > ~/.zsh_history && poweroff'
alias reboot='echo "_" > ~/.zsh_history && reboot'
